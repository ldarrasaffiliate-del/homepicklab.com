import nextCoreWebVitals from 'eslint-config-next/core-web-vitals';

// Next 16 ships a flat ESLint config via `eslint-config-next/*`.
// Re-export it so `eslint` works without `next lint`.
export default nextCoreWebVitals;
